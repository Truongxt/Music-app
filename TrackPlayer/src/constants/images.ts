import unknownTrackImage from '@/assets/unknow/unknow_track.png';
import unknowUserImage from '@/assets/unknow/unknow_user.jpg';
import { Image } from 'react-native';

export const unknownTrackImageUri = Image.resolveAssetSource(unknownTrackImage).uri
export const unkownUserUri = Image.resolveAssetSource(unknowUserImage).uri